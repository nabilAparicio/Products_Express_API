const config = {
  port: 8005,
  nodeEnv: "development",
  db: {
    port: 5432,
    username: "admin",
    password: "root",
    host: "localhost",
    name: "products_crud",
  },
};

module.exports = config;
